#include <stdio.h>
#include <math.h>

int main()
{
    int current_year;

    while ((floor(log10(current_year)) + 1) < 4 || (floor(log10(current_year)) + 1) > 4)
    {
        printf("Enter starting year: ");
        scanf("%d", &current_year);
    }

    printf()
}